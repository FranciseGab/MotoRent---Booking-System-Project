# name: Frontend Unit Tests

# on:
#   push:
#     branches: ["main"]
#   pull_request:
#     branches: ["main"]

# jobs:
#   karma-tests:
#     runs-on: ubuntu-latest

#     defaults:
#       run:
#         working-directory: Frontend

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20.x'
#           cache: 'npm'
#           cache-dependency-path: Frontend/package-lock.json

#       - name: Install dependencies
#         run: npm ci

#       - name: Run tests
#         run: npx ng test --watch=false

#       - name: Upload coverage report
#         uses: actions/upload-artifact@v4
#         with:
#           name: frontend-coverage-report
#           path: Frontend/coverage/

name: Frontend Unit Tests

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  karma-tests:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: Frontend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
          cache-dependency-path: Frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npx ng test --watch=false

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: frontend-coverage-report
          path: Frontend/coverage/

  deploy:
    runs-on: ubuntu-latest
    needs: karma-tests
    if: github.ref == 'refs/heads/main'

    defaults:
      run:
        working-directory: Frontend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
          cache-dependency-path: Frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build Angular
        run: npx ng build --base-href /MotoRent---Booking-System-Project/

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: Frontend/dist/frontend/browser
      
      - name: Build Angular
        run: npx ng build --base-href /MotoRent---Booking-System-Project/

      - name: Show dist folder
        run: ls -la dist/
        working-directory: Frontend

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: Frontend/dist/Client